<div class="wysiwyg wysiwyg--{{location}}">
<div class="wysiwyg__content--{{location}}">
	{% if location == 'home' %}
		<h1 class="title-large--{{location}}">{{ post.title }}</h1>
	{% endif %}
    {{ post.content}}
 </div>
</div>